
describe('jscore', () => {
    describe('initialization', () => {
        test('Dependency check fails', async () => {
        })
        test('Invalid config fails', async () => {
        })
    })
    describe('module management', () => {
        describe('start', () => {
            test('Errors gracefully on module failure', async () => {
            })
        })
    })
    describe('lib management', () => {
        describe('start', () => {
            test('Errors gracefully on module failure', async () => {
            })
        })
    })
    describe('store management', () => {
        describe('start', () => {
            test('Errors gracefully on module failure', async () => {
            })
        })
    })
})